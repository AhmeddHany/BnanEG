

@using Bnan.Core.Extensions
@model Tuple<IEnumerable<CrMasSupRenterEmployer>, List<List<string>>>
@inject IViewLocalizer localizer
@inject IOptions<RequestLocalizationOptions> options
@{
    var requestCulture = Context.Features.Get<IRequestCultureFeature>();
    ViewData["returnUrl"] = string.IsNullOrEmpty(Context.Request.Path) ? "~/" : $"~{Context.Request.Path.Value}";
}
@if (requestCulture?.RequestCulture.UICulture.Name == "en-US")
{
    <div class="col-sm-12">
        @* For-scrolling *@
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap w-100 dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px; ">
            <thead>
                <tr>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["No"]</th>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["RenterEmployerCode"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["RenterEmployerName"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Status"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model.Item1)
                {
                    @if (item.CrMasSupRenterEmployerStatus == @Status.Deleted || item.CrMasSupRenterEmployerStatus == @Status.Hold)
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterEmployer", new { id = item.CrMasSupRenterEmployerCode })'" style="cursor:pointer;color:red">
                            <td id="_Nc">@no</td>
                            <td id="_Nc">@item.CrMasSupRenterEmployerCode</td>
                            <td>@item.CrMasSupRenterEmployerEnName</td>
                            <td id="_Nc">
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if (row[0] == @item.CrMasSupRenterEmployerCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>

                                @if (@item.CrMasSupRenterEmployerStatus == Status.Deleted)
                                {
                                    @localizer["Deleted"]
                                }
                                else if (@item.CrMasSupRenterEmployerStatus == Status.Hold)
                                {
                                    @localizer["Hold"]
                                }
                            </td>
                        </tr>
                    }
                    else
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterEmployer", new { id = item.CrMasSupRenterEmployerCode })'">
                            <td id="_Nc">@no</td>
                            <td id="_Nc">@item.CrMasSupRenterEmployerCode</td>
                            <td>@item.CrMasSupRenterEmployerEnName</td>
                            <td id="_Nc">
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if (row[0] == @item.CrMasSupRenterEmployerCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>

                                @if (@item.CrMasSupRenterEmployerStatus == Status.Active)
                                {
                                    @localizer["Active"]
                                }
                            </td>
                        </tr>
                    }
                    no++;
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="col-sm-12">
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap w-100 dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px;">
            <thead>
                <tr>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["No"]</th>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["RenterEmployerCode"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["RenterEmployerName"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Status"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model.Item1)
                {
                    @if (item.CrMasSupRenterEmployerStatus == @Status.Deleted || item.CrMasSupRenterEmployerStatus == @Status.Hold)
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterEmployer", new { id = item.CrMasSupRenterEmployerCode })'" style="cursor:pointer;color:red">
                            <td id="_Nc">&emsp; @no</td>
                            <td id="_Nc">&emsp; @item.CrMasSupRenterEmployerCode</td>
                            <td>&emsp; @item.CrMasSupRenterEmployerArName</td>
                            <td id="_Nc">
                                &emsp;
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if (row[0] == @item.CrMasSupRenterEmployerCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>
                                &emsp;
                                @if (@item.CrMasSupRenterEmployerStatus == Status.Deleted)
                                {
                                    @localizer["Deleted"]
                                }
                                else if (@item.CrMasSupRenterEmployerStatus == Status.Hold)
                                {
                                    @localizer["Hold"]
                                }
                            </td>
                        </tr>
                    }
                    else
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterEmployer", new { id = item.CrMasSupRenterEmployerCode })'">
                            <td id="_Nc">&emsp; @no</td>
                            <td id="_Nc">&emsp; @item.CrMasSupRenterEmployerCode</td>
                            <td>&emsp; @item.CrMasSupRenterEmployerArName</td>
                            <td id="_Nc">
                                &emsp;
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if (row[0] == @item.CrMasSupRenterEmployerCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>
                                &emsp;
                                @if (@item.CrMasSupRenterEmployerStatus == Status.Active)
                                {
                                    @localizer["Active"]
                                }
                            </td>
                        </tr>
                    }
                    no++;
                }
            </tbody>
        </table>
    </div>
}