@using Bnan.Core.Extensions 
@model Tuple<IEnumerable<CrMasSupRenterMembership>, List<List<string>>>
@inject IViewLocalizer localizer
@inject IOptions<RequestLocalizationOptions> options
@{
    var requestCulture = Context.Features.Get<IRequestCultureFeature>();
       ViewData["returnUrl"] = string.IsNullOrEmpty(Context.Request.Path) ? "~/" : $"~{Context.Request.Path.Value}"; 
}
@if (requestCulture?.RequestCulture.UICulture.Name == "en-US")
{
    <div class="col-sm-12">
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap w-100 dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px;">
            <thead>
                <tr >
                    <th aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["No"]</th>
                    <th aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["RenterMembershipCode"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["RenterMembershipName"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Status"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model.Item1)
                {
                    @if (item.CrMasSupRenterMembershipStatus == @Status.Deleted || item.CrMasSupRenterMembershipStatus == @Status.Hold)
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterMembership", new { id = item.CrMasSupRenterMembershipCode })'" style="cursor:pointer;color:red">
                            <td>@no</td>
                            <td>@item.CrMasSupRenterMembershipCode</td>
                            <td>@item.CrMasSupRenterMembershipEnName</td>
                            <td>
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if(row[0] == @item.CrMasSupRenterMembershipCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>
                                &emsp;
                                @if (@item.CrMasSupRenterMembershipStatus == Status.Deleted)
                                {
                                    @localizer["Deleted"]
                                }
                                else if (@item.CrMasSupRenterMembershipStatus == Status.Hold)
                                {
                                    @localizer["Hold"]
                                }
                            </td>
                        </tr>
                    }
                    else
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterMembership", new { id = item.CrMasSupRenterMembershipCode })'">
                            <td>@no</td>
                            <td>@item.CrMasSupRenterMembershipCode</td>
                            <td>@item.CrMasSupRenterMembershipEnName</td>
                            <td>
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if (row[0] == @item.CrMasSupRenterMembershipCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>
                                &emsp;
                                @if (@item.CrMasSupRenterMembershipStatus == Status.Active)
                                {
                                    @localizer["Active"]
                                }
                            </td>
                        </tr>
                    }
                    no++;
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="col-sm-12">
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap w-100 dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px;">
            <thead>
                <tr role="row">
                    <th aria-sort="ascending" aria-label="Name: activate to sort column descending" >@localizer["No"]</th>
                    <th aria-sort="ascending" aria-label="Name: activate to sort column descending" >@localizer["RenterMembershipCode"]</th>
                    <th aria-label="Position: activate to sort column ascending" >@localizer["RenterMembershipName"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Count"]</th>
                    <th aria-label="Position: activate to sort column ascending" >@localizer["Status"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model.Item1)
                {
                    @if (item.CrMasSupRenterMembershipStatus == @Status.Deleted || item.CrMasSupRenterMembershipStatus == @Status.Hold)
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterMembership", new { id = item.CrMasSupRenterMembershipCode })'" style="cursor:pointer;color:red">
                            <td>&emsp; @no</td>
                            <td>&emsp; @item.CrMasSupRenterMembershipCode</td>
                            <td>&emsp; @item.CrMasSupRenterMembershipArName</td>
                            <td>
                                &emsp;&emsp;
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if (row[0] == @item.CrMasSupRenterMembershipCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>
                                &emsp;&emsp;
                                @if (@item.CrMasSupRenterMembershipStatus == Status.Deleted)
                                {
                                    @localizer["Deleted"]
                                }
                                else if (@item.CrMasSupRenterMembershipStatus == Status.Hold)
                                {
                                    @localizer["Hold"]
                                }
                            </td>
                        </tr>
                    }
                    else
                    {
                        <tr onclick="location.href='@Url.Action("Edit", "RenterMembership", new { id = item.CrMasSupRenterMembershipCode })'">
                            <td>&emsp; @no</td>
                            <td>&emsp; @item.CrMasSupRenterMembershipCode</td>
                            <td>&emsp; @item.CrMasSupRenterMembershipArName</td>
                            <td>
                                &emsp;&emsp;
                                @if (Model.Item2 != null)
                                {
                                    @foreach (var row in Model.Item2)
                                    {
                                        if (row[0] == @item.CrMasSupRenterMembershipCode)
                                        {
                                            @row[1]
                                        }

                                    }
                                }
                            </td>
                            <td>
                                &emsp;&emsp;
                                @if (@item.CrMasSupRenterMembershipStatus == Status.Active)
                                {
                                    @localizer["Active"]
                                }
                            </td>
                        </tr>
                    }
                    no++;
                }
            </tbody>
        </table>
    </div>
}